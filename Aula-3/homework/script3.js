let userAgeInput = prompt ("whats is your age ?")
let age = Number (userAgeInput)

if (userAgeInput) {
    //userAgeInput string with length
if (!Number.isNaN(age)) {
    //number not NaN
} else {
    //Ag is NaN
}
} else {
//userAgeInpit null or ''
}

console.log(age)


function checkIsAbove100(num){

if (typeof num === 'number' && !Number.isNaN(num)) {

}
}